--- # Construir una app en Debian 12
- name: Configurar repositorios NPM locales
  shell: npm set registry https://registry.upat.agetic.gob.bo/

- name: Instalar las dependencias con npm
  shell: cd {{ projectPath }} &&  npm install

